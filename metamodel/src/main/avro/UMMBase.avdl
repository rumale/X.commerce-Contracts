@namespace("com.x.ocl")
protocol BaseModule {

	@abstract("true")
	record Package {
		string version;
		string uniqueIdentifier;
		union{null,string} status = null;

		array<string> owners;
		array<string> references;		
	}
}